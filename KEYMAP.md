# MODU-C 키 배열

키맵 원본은 [`modu-module/boards/shields/modu/modu.keymap`](modu-module/boards/shields/modu/modu.keymap) 입니다.
이 문서는 그 파일을 사람이 읽기 쉽게 옮긴 것이므로, 키맵을 고치면 여기도 함께 갱신해 주세요.

## 한눈에 보기

- 좌우 분리형, 한쪽당 **6열 × 5행 + 엄지 키**
- 실제 스위치 수는 **60개** (알파 48 + 아래행 6 + 엄지 6)
- 양쪽에 **트랙볼**(PMW3610) 1개씩
- **Windows 모드**와 **Mac 모드**가 있고, `MO` + `V`로 전환합니다

Windows 모드가 기본이지만, 마지막으로 고른 모드는 재부팅해도 유지됩니다.

> **오른쪽 엄지의 가장 바깥 한 칸은 스위치가 없습니다.** 모듈이 그 끝을 차지합니다. 아래 그림에서 `/////`로 표시했습니다.

---

## Windows 모드

```
┌─────┬─────┬─────┬─────┬─────┬─────┐   ┌─────┬─────┬─────┬─────┬─────┬─────┐
│ ESC │  1  │  2  │  3  │  4  │  5  │   │  6  │  7  │  8  │  9  │  0  │BSPC │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│ TAB │  Q  │  W  │  E  │  R  │  T  │   │  Y  │  U  │  I  │  O  │  P  │  \  │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│CAPS │  A  │  S  │  D  │  F  │  G  │   │  H  │  J  │  K  │  L  │  ;  │ RET │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│LSHFT│  Z  │  X  │  C  │  V  │  B  │   │  N  │  M  │  ,  │  .  │  /  │RSHFT│
├─────┼─────┼─────┼─────┴─────┴─────┘   └─────┴─────┴─────┼─────┼─────┼─────┤
│LCTRL│LALT │LGUI │                                       │RALT │RCTRL│ DEL │
└─────┴─────┴─────┘                                       └─────┴─────┴─────┘
                  ┌─────┬─────┬─────┐   ┌─────┬─────┬─────┬─────┐
                  │ MO  │SPACE│ RET │   │BSPC │SPACE│ MO  │/////│
                  └─────┴─────┴─────┘   └─────┴─────┴─────┴─────┘
```

수식키 배치

```
  Ctrl   Alt   Win  │ 스페이스 │  Alt   Ctrl
```

- `MO`는 누르고 있는 동안만 [기능 레이어](#기능-레이어-mo)를 켭니다. 좌우 어느 쪽이든 같습니다.
- 한/영은 **`RALT`** 입니다. 한국어 101키 키보드에서 오른쪽 Alt 자리가 한/영 키인 것과 같습니다.
- `P` 오른쪽의 `\` 키는 한글 입력 상태에서 **`₩`** 를 냅니다. 한국어 키보드의 `\`/`₩` 키와 같습니다.

---

## Mac 모드

`MO` + `V`로 켭니다. `*` 표시한 **2개 키만** 달라지고 나머지는 완전히 동일합니다.

```
┌─────┬─────┬─────┬─────┬─────┬─────┐   ┌─────┬─────┬─────┬─────┬─────┬─────┐
│ ESC │  1  │  2  │  3  │  4  │  5  │   │  6  │  7  │  8  │  9  │  0  │BSPC │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│ TAB │  Q  │  W  │  E  │  R  │  T  │   │  Y  │  U  │  I  │  O  │  P  │  \  │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│CAPS │  A  │  S  │  D  │  F  │  G  │   │  H  │  J  │  K  │  L  │  ;  │ RET │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│LSHFT│  Z  │  X  │  C  │  V  │  B  │   │  N  │  M  │  ,  │  .  │  /  │RSHFT│
├─────┼─────┼─────┼─────┴─────┴─────┘   └─────┴─────┴─────┼─────┼─────┼─────┤
│LCTRL│LALT │LGUI │                                       │RGUI*│RALT*│ DEL │
└─────┴─────┴─────┘                                       └─────┴─────┴─────┘
                  ┌─────┬─────┬─────┐   ┌─────┬─────┬─────┬─────┐
                  │ MO  │SPACE│ RET │   │BSPC │SPACE│ MO  │/////│
                  └─────┴─────┴─────┘   └─────┴─────┴─────┴─────┘
```

수식키 배치 — 좌우 대칭입니다.

```
  Ctrl  Option Command │ 스페이스 │ Command Option
```

| 위치 | Windows | Mac | 이유 |
| --- | --- | --- | --- |
| 스페이스 옆 (오른쪽) | `RALT` | `RGUI` | Mac은 스페이스 양옆이 Command |
| 그 바깥 | `RCTRL` | `RALT` | 그 바깥이 Option |

왼쪽 아래는 원래 `Ctrl · Alt · GUI` 순서라 Mac의 `Ctrl · Option · Command`와 그대로 맞아서 바꿀 필요가 없습니다.

한/영은 **`CAPS`** 로 전환합니다. macOS 기본 동작이자 매직키보드와 같은 위치입니다.

### 모드는 기억됩니다

ZMK 기본 동작으로는 토글 상태가 전원을 끄면 사라지지만, 이 저장소의 [`os_mode`](modu-module/src/os_mode/) 모듈이 상태를 저장했다가 부팅할 때 복원합니다. 토글하고 약 0.5초 뒤에 기록되므로, 누르자마자 전원을 뽑으면 저장되지 않을 수 있습니다.

---

## 기능 레이어 (`MO`)

`MO`를 **누르고 있는 동안만** 켜집니다. **Windows 모드와 Mac 모드에서 완전히 동일합니다.**

```
┌─────┬─────┬─────┬─────┬─────┬─────┐   ┌─────┬─────┬─────┬─────┬─────┬─────┐
│ F1  │ F2  │ F3  │ F4  │ F5  │ F6  │   │ F7  │ F8  │ F9  │ F10 │ F11 │ F12 │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│BTCLR│ BT0 │ BT1 │ BT2 │STUD │     │   │MCLK │     │     │  `  │  [  │  ]  │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│     │     │     │     │     │     │   │RCLK │     │     │  -  │  =  │  '  │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│     │  ↑  │     │     │ MAC │     │   │LCLK │     │     │     │  ↑  │     │
├─────┼─────┼─────┼─────┴─────┴─────┘   └─────┴─────┴─────┼─────┼─────┼─────┤
│  ←  │  ↓  │  →  │                                       │  ←  │  ↓  │  →  │
└─────┴─────┴─────┘                                       └─────┴─────┴─────┘
                  ┌─────┬─────┬─────┐   ┌─────┬─────┬─────┬─────┐
                  │(MO) │     │     │   │     │     │(MO) │/////│
                  └─────┴─────┴─────┘   └─────┴─────┴─────┴─────┘
```

### 십자 방향키

좌우가 같은 구조입니다. 아래행 3칸이 `←↓→`, 그 위 가운데 칸이 `↑`.

| | `↑` | `←` `↓` `→` |
| --- | --- | --- |
| 왼손 | `Z` 자리 | 맨 아래 왼쪽 3칸 (`LCTRL` `LALT` `LGUI` 자리) |
| 오른손 | `/` 자리 | 맨 아래 오른쪽 3칸 (`RALT` `RCTRL` `DEL` 자리) |

`↑`가 `↓` 바로 위 칸에 오도록 열을 맞췄기 때문에 온전한 십자가 됩니다.

### 기호

기본 레이어에 자리가 없는 기호는 오른쪽 두 열에 모아뒀습니다. 전체 목록과 Shift 조합은 아래 [기호 찾아보기](#기호-찾아보기)를 보세요.

### 그 밖에

- **왼손** — F1~F12, 블루투스
- **오른손** — 마우스 클릭 (`LCLK` 좌클릭 · `MCLK` 휠클릭 · `RCLK` 우클릭)
- `MAC` — **Mac 모드 토글**. `V` 자리에 있습니다
- `BTCLR` — 블루투스 초기화. 좌우가 서로 못 붙을 때 양쪽에서 누르고 다시 페어링하세요
- `BT0`~`BT2` — 블루투스 프로파일 전환 (기기 3대까지)
- `STUD` — ZMK Studio 잠금 해제. `R` 자리에 있습니다. ZMK Studio는 USB로 연결해 키맵을 실시간으로 바꾸는 도구이고, 이 키가 없으면 연결은 되어도 변경이 거부됩니다
- 빈칸은 아래 레이어의 키가 그대로 나갑니다

### 재시작 조합

`MO`를 누른 채로 **`5` + `6` + `J`** 를 0.5초 안에 같이 누르면 키보드가
재시작합니다. 좌우가 이상하게 붙었을 때 전원을 뽑지 않고 되돌리는 용도입니다.

- 전원을 껐다 켜는 것과 같습니다. **설정이나 키맵은 지워지지 않습니다**
- `MO`를 누르고 있을 때만 동작하므로 평소 타이핑 중에는 걸리지 않습니다

---

## 기호 찾아보기

키캡에 안 보이는 기호도 전부 입력할 수 있습니다. 규칙은 두 가지뿐입니다.

1. **`Shift`를 누르면** 그 키의 위쪽 문자가 나옵니다 — 일반 키보드와 같습니다
2. **기본 레이어에 없는 기호는 `MO`를 누른 채** 칩니다

둘이 겹치면 `Shift` + `MO` + 키 세 개를 함께 누릅니다. 예를 들어 `"`는 `Shift`+`MO`+`'`자리입니다.

### Shift를 눌렀을 때 (기본 레이어)

```
┌─────┬─────┬─────┬─────┬─────┬─────┐   ┌─────┬─────┬─────┬─────┬─────┬─────┐
│ ESC │  !  │  @  │  #  │  $  │  %  │   │  ^  │  &  │  *  │  (  │  )  │BSPC │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│ TAB │  Q  │  W  │  E  │  R  │  T  │   │  Y  │  U  │  I  │  O  │  P  │  |  │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│CAPS │  A  │  S  │  D  │  F  │  G  │   │  H  │  J  │  K  │  L  │  :  │ RET │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│LSHFT│  Z  │  X  │  C  │  V  │  B  │   │  N  │  M  │  <  │  >  │  ?  │RSHFT│
├─────┼─────┼─────┼─────┴─────┴─────┘   └─────┴─────┴─────┼─────┼─────┼─────┤
│LCTRL│LALT │LGUI │                                       │RALT │RCTRL│ DEL │
└─────┴─────┴─────┘                                       └─────┴─────┴─────┘
                  ┌─────┬─────┬─────┐   ┌─────┬─────┬─────┬─────┐
                  │ MO  │SPACE│ RET │   │BSPC │SPACE│ MO  │/////│
                  └─────┴─────┴─────┘   └─────┴─────┴─────┴─────┘
```

알파벳은 대문자가 됩니다. 맨 아래 행과 엄지 키는 Shift로 달라지지 않습니다.

### Shift + MO를 눌렀을 때 (기호 영역)

기능 레이어에서 오른손 기호 6개만 위쪽 문자로 바뀝니다. 나머지는 그대로입니다.

```
┌─────┬─────┬─────┬─────┬─────┬─────┐   ┌─────┬─────┬─────┬─────┬─────┬─────┐
│ F1  │ F2  │ F3  │ F4  │ F5  │ F6  │   │ F7  │ F8  │ F9  │ F10 │ F11 │ F12 │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│BTCLR│ BT0 │ BT1 │ BT2 │STUD │     │   │MCLK │     │     │  ~  │  {  │  }  │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│     │     │     │     │     │     │   │RCLK │     │     │  _  │  +  │  "  │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│     │  ↑  │     │     │ MAC │     │   │LCLK │     │     │     │  ↑  │     │
├─────┼─────┼─────┼─────┴─────┴─────┘   └─────┴─────┴─────┼─────┼─────┼─────┤
│  ←  │  ↓  │  →  │                                       │  ←  │  ↓  │  →  │
└─────┴─────┴─────┘                                       └─────┴─────┴─────┘
                  ┌─────┬─────┬─────┐   ┌─────┬─────┬─────┬─────┐
                  │(MO) │     │     │   │     │     │(MO) │/////│
                  └─────┴─────┴─────┘   └─────┴─────┴─────┴─────┘
```

`MO`만 눌렀을 때와 비교하면 이 6칸만 달라집니다.

| `MO` | `Shift` + `MO` |
| --- | --- |
| `` ` `` `[` `]` | `~` `{` `}` |
| `-` `=` `'` | `_` `+` `"` |

### 전체 목록

기준이 되는 키 이름은 **기본 레이어에서 그 자리에 적힌 것**입니다.

| 문자 | 치는 법 |
| --- | --- |
| `!` `@` `#` `$` `%` `^` `&` `*` `(` `)` | `Shift` + 숫자 |
| `:` | `Shift` + `;` |
| `<` `>` `?` | `Shift` + `,` `.` `/` |
| `\` | `P` 오른쪽 키 |
| `\|` | `Shift` + `P` 오른쪽 키 |
| `₩` | **한글 입력 상태**에서 `P` 오른쪽 키 |
| `` ` `` | `MO` + `O` |
| `~` | `Shift` + `MO` + `O` |
| `-` | `MO` + `L` |
| `_` | `Shift` + `MO` + `L` |
| `=` | `MO` + `;` |
| `+` | `Shift` + `MO` + `;` |
| `[` | `MO` + `P` |
| `]` | `MO` + `P` 오른쪽 키 |
| `{` | `Shift` + `MO` + `P` |
| `}` | `Shift` + `MO` + `P` 오른쪽 키 |
| `'` | `MO` + `RET` 자리 |
| `"` | `Shift` + `MO` + `RET` 자리 |

### 세 개 조합이 불편하다면

`Shift` + `MO` + 키는 손가락 세 개를 써야 합니다. 왼손 새끼로 `LSHFT`, 왼손 엄지로 `MO`, 오른손으로 해당 키를 누르면 겹치지 않습니다.

그래도 불편하면 기능 레이어 오른손의 **빈 자리 6개**에 자주 쓰는 문자를 직접 넣을 수 있습니다. 예를 들어 `"`를 빈 자리에 배치하면 `MO` + 한 키로 끝납니다.

---

## 일본어 입력

> 이 절은 **설명이고, 키맵을 바꾸지는 않습니다.** 지금 배열 그대로 일본어를 칠 수
> 있습니다.

### 먼저 — 가타카나는 별도 배열이 아닙니다

히라가나와 가타카나는 **같은 키를 씁니다.** 다른 건 배열이 아니라 변환 모드입니다.
`か`를 치고 가타카나로 바꾸고 싶으면 변환 중에 **`F7`** (기능 레이어에 있습니다)을
누르면 `カ`가 됩니다. 배열이 두 벌 있는 게 아니라 **한 벌을 놓고 결과를 바꾸는**
구조입니다.

| 원하는 것 | 방법 |
|---|---|
| 입력 중인 글자를 가타카나로 | 변환 중 **`F7`** |
| 계속 가타카나로 치기 | IME의 카타카나 모드 (Windows는 `무변환` 반복, macOS는 입력 소스에서 선택) |
| 히라가나로 되돌리기 | 변환 중 **`F6`** |

`F6`~`F10`이 전부 기능 레이어 숫자행에 있으므로 `MO` + 해당 키로 누릅니다.

### 입력 방식은 두 가지입니다

| | 어떻게 치나 | MODU에서 |
|---|---|---|
| **ローマ字入力** (로마자 입력) | `k` `a` → `か` | ✅ **지금 그대로 됩니다** |
| **かな入力** (가나 직접 입력) | `T` 자리 → `か` | ⚠️ 키가 7개 모자랍니다 (아래) |

일본에서도 다수가 로마자 입력을 씁니다. **바꿀 게 없는 쪽이 실용적입니다.**

### 로마자 입력 — 오십음도

자음 키 + 모음 키 순서로 칩니다. 지금 배열 그대로입니다.

```
      A      I      U      E      O
     ─────────────────────────────────
     あ a   い i   う u   え e   お o
 K   か ka  き ki  く ku  け ke  こ ko
 S   さ sa  し si  す su  せ se  そ so
 T   た ta  ち ti  つ tu  て te  と to
 N   な na  に ni  ぬ nu  ね ne  の no
 H   は ha  ひ hi  ふ hu  へ he  ほ ho
 M   ま ma  み mi  む mu  め me  も mo
 Y   や ya          ゆ yu         よ yo
 R   ら ra  り ri  る ru  れ re  ろ ro
 W   わ wa                        を wo
 N   ん nn (또는 n을 두 번)
```

탁음·반탁음·요음도 같은 방식입니다.

| 예 | 침 | 결과 |
|---|---|---|
| 탁음 | `g` `a` | が |
| 반탁음 | `p` `a` | ぱ |
| 요음 | `k` `y` `a` | きゃ |
| 촉음 | `k` `k` `a` | っか (자음 반복) |
| 장음 | `-` | ー (`MO` + `L`) |

장음 `ー`는 `-` 와 같은 키라 **`MO` + `L`** 입니다.

⚠️ **일본어에서 아주 자주 나오는데 `MO`를 같이 눌러야 합니다.** 한때 검지 홈
자리로 옮겨봤지만 `-`는 프로그래밍에서도 자주 쓰는 키라 손이 `L` 자리를 기억하고
있어서 되돌렸습니다. 일본어를 주력으로 쓰시게 되면 그때 다시 볼 일입니다.

### IME 전환 키

한/영과 달리 **일본어 IME 전환은 OS 설정에 따릅니다.** 지금 배열에 전용 키는
없습니다.

| | 전환 |
|---|---|
| Windows | `Alt` + `` ` `` (`MO` + `O` 자리) — 또는 IME 설정에서 변경 |
| macOS | `Ctrl` + `Space` 로 입력 소스 순환 (시스템 설정 기본값) |

전용 키를 원하면 ZMK에 코드가 있습니다. 기능 레이어 빈 자리에 넣을 수 있습니다.

| 넣을 것 | ZMK | 무엇 |
|---|---|---|
| `&kp LANG1` | かな | macOS 일본어 IME 켜기 |
| `&kp LANG2` | 英数 | macOS 영문으로 |
| `&kp INT_KANA` | カタカナひらがな | JIS 키보드의 그 키 |
| `&kp INT_HENKAN` | 変換 | |
| `&kp INT_MUHENKAN` | 無変換 | |

### かな入力(JIS) 배열 — 참고용

가나를 직접 치는 배열입니다. 일본 JIS 키보드 각인이 이것입니다.

```
┌─────┬─────┬─────┬─────┬─────┬─────┐   ┌─────┬─────┬─────┬─────┬─────┬─────┐
│ ESC │ ぬ  │ ふ  │ あ  │ う  │ え  │   │ お  │ や  │ ゆ  │ よ  │ わ  │BSPC │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│ TAB │ た  │ て  │ い  │ す  │ か  │   │ ん  │ な  │ に  │ ら  │ せ  │ ゛  │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│CAPS │ ち  │ と  │ し  │ は  │ き  │   │ く  │ ま  │ の  │ り  │ れ  │ RET │
├─────┼─────┼─────┼─────┼─────┼─────┤   ├─────┼─────┼─────┼─────┼─────┼─────┤
│LSHFT│ つ  │ さ  │ そ  │ ひ  │ こ  │   │ み  │ も  │ ね  │ る  │ め  │RSHFT│
├─────┼─────┼─────┼─────┴─────┴─────┘   └─────┴─────┴─────┼─────┼─────┼─────┤
│LCTRL│LALT │LGUI │                                       │RALT │RCTRL│ DEL │
└─────┴─────┴─────┘                                       └─────┴─────┴─────┘
```

Shift를 누르면 작은 글자와 문장부호가 나옵니다.

| Shift + | 나오는 것 |
|---|---|
| `あ` `う` `え` `お` 자리 | ぁ ぅ ぇ ぉ |
| `や` `ゆ` `よ` 자리 | ゃ ゅ ょ |
| `つ` 자리 | っ |
| `わ` 자리 | を |
| `ね` `る` `め` 자리 | 、 。 ・ |

#### ⚠️ MODU에는 7자리가 모자랍니다

JIS 키보드는 숫자행이 13칸이고 `;` 오른쪽과 오른쪽 Shift 왼쪽에도 키가 있습니다.
MODU는 12칸이고 그 자리들이 없습니다.

| 못 들어간 것 | JIS에서의 자리 |
|---|---|
| `ほ` `へ` `ー` | 숫자행 `-` `^` `¥` |
| `゜` (반탁점) | `[` |
| `け` `む` | `:` `]` |
| `ろ` | 오른쪽 Shift 왼쪽 |

**`ー`(장음)와 `゜`(반탁점)가 빠지는 게 특히 치명적입니다** — 둘 다 일상적으로
쓰입니다. 기능 레이어로 빼면 칠 수는 있지만, 그 시점에 **로마자 입력보다 불편해집니다.**

**결론: MODU에서 일본어는 로마자 입력을 쓰세요.** 위 배열은 JIS 키보드가 어떻게
생겼는지 참고용입니다.

---

## 레이어 구조

키맵 파일에는 레이어가 3장 있습니다.

| 번호 | 이름 | 역할 |
| --- | --- | --- |
| 0 | `default_layer` | Windows 모드 전체 배열 |
| 1 | `mac_layer` | Mac에서 달라지는 **2개 키만** 정의, 나머지는 통과 |
| 2 | `lower_layer` | 기능 레이어 |

ZMK는 켜져 있는 레이어 중 **번호가 큰 쪽이 이깁니다.** `mac_layer`를 일부러 `lower_layer` 아래(1번)에 둔 이유가 여기 있습니다. 만약 2번에 뒀다면 Mac 모드에서 `MO`를 눌렀을 때 방향키 자리가 `RGUI`/`RALT`에 가려졌을 것입니다.

---

## 엄지 키 방향 전환

부팅할 때 **P0.08 핀 상태**에 따라 각 반쪽의 엄지 1번과 3번 키가 서로 바뀝니다. 좌우 대칭 조립과 미러 조립 양쪽을 지원하기 위한 것으로, [`alt_thumb_kscan`](modu-module/src/alt_thumb_kscan/) 모듈이 처리합니다.

| P0.08 | 왼쪽 엄지 (바깥→안) | 오른쪽 엄지 (안→바깥) |
| --- | --- | --- |
| 열림 (기본) | `MO` · `SPACE` · `RET` | `BSPC` · `SPACE` · `MO` |
| 접지 | `RET` · `SPACE` · `MO` | `MO` · `SPACE` · `BSPC` |

양쪽 다 `SPACE`는 가운데를 유지하고 `MO`와 편집키만 자리를 바꿉니다. 어느 방향으로 조립해도 쓸 만합니다.

같은 핀으로 트랙볼의 회전 보정 방향도 함께 바뀝니다.

## 트랙볼

양쪽에 하나씩 있고 해상도는 600 CPI입니다. 왼쪽(중심)은 자기 트랙볼을 직접 HID로 내보내고, 오른쪽은 블루투스로 왼쪽에 넘겨서 왼쪽이 대신 내보냅니다. 두 반쪽 모두 45° 회전 보정이 적용되어 있습니다.
